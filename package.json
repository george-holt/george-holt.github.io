{
  "name": "george-holt-website",
  "version": "1.0.0",
  "description": "George Holt's professional website",
  "main": "index.html",
  "scripts": {
    "build": "node build.js",
    "build:watch": "node build.js --watch",
    "clean": "rimraf dist",
    "dev": "node build.js --dev",
    "test": "npm run build && node scripts/test-lighthouse.js",
    "test:lighthouse": "npm run build && node scripts/test-lighthouse.js",
    "test:ci": "npm run build && lhci autorun",
    "test:quick": "npm run build && lhci autorun --config=./lighthouserc.js"
  },
  "devDependencies": {
    "@lhci/cli": "^0.15.1",
    "chart.js": "^4.5.0",
    "chokidar": "^3.5.3",
    "clean-css": "^5.3.2",
    "esbuild": "^0.21.5",
    "fs-extra": "^11.1.1",
    "glob": "^10.5.0",
    "html-minifier": "^4.0.0",
    "http-server": "^14.1.1",
    "lighthouse": "^12.8.1",
    "node-fetch": "^3.3.2",
    "purgecss": "^5.0.0",
    "rimraf": "^5.0.1",
    "terser": "^5.19.2"
  },
  "keywords": [
    "website",
    "portfolio",
    "resume"
  ],
  "author": "George Holt",
  "license": "MIT"
}
